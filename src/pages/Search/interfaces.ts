export interface SearchField {
  id: string;
  field_id: number;
  type_code: string;
  format_name: string;
  description: string;
  from: string;
  to: string;
  fieldName: string;
  isSelected: boolean;
}
